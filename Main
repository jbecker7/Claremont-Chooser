#Claremont Restaurant Chooser--Jonathan Becker 2022
import time
import random
import pandas as pd
import numpy as np

def main():
    restaurants = pd.read_csv('Claremont_DB1.csv')

    print("What cost range would you like to search for?")
    cost_chosen=input()

    print("What type of cuisine would you like to search for?")
    cuisine_chosen=input()

    print("What tags would you like to search for?")
    tag_chosen=input()

    if cost_chosen=="Any":
        r1=restaurants
    else:
        r1 = restaurants.loc[restaurants['Cost']==cost_chosen] # 

    if cuisine_chosen=="Any":
        r2=r1
    else:
        r2 = r1.loc[r1['Style 1']==style_chosen | r1['Style 2']==style_chosen | r1['Style 3'==style_chosen]] 
    
    if tag_chosen=="Any":
        r3=r2
    else:
        r3 = r2.loc[r2['Particularly Good For']==particularly_good_for] # get all restaurants with the chosen good for

    r3.sample(n=None, frac=None, replace=False, weights=None, random_state=None, axis=None, ignore_index=False ) # shuffle the data